# 🚀 免费部署步骤 - Railway

## ✅ 已完成
- ✅ Git 仓库已初始化
- ✅ 所有文件已提交
- ✅ 部署配置文件已准备好

## 📋 下一步操作

### 步骤 1：在 GitHub 创建仓库

1. **访问 GitHub**
   - 打开：https://github.com/new
   - 或者：https://github.com → 点击右上角 "+" → "New repository"

2. **创建新仓库**
   - Repository name: `ai-sales-coach`
   - Description: `AI Sales Coach Platform`
   - 选择 **Public**（公开）或 **Private**（私有）
   - ⚠️ **不要**勾选：
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
   - 点击 "Create repository"

3. **复制仓库 URL**
   - GitHub 会显示类似：`https://github.com/你的用户名/ai-sales-coach.git`
   - 复制这个 URL

### 步骤 2：推送到 GitHub

在终端运行（替换成你的 GitHub 用户名）：

```bash
cd /Users/zhangkexiang/Desktop/ai-sales-coach

# 连接远程仓库（替换成你的 GitHub 用户名）
git remote add origin https://github.com/你的用户名/ai-sales-coach.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

如果提示输入用户名密码，使用：
- 用户名：你的 GitHub 用户名
- 密码：GitHub Personal Access Token（不是密码！）
  - 获取方法：GitHub → Settings → Developer settings → Personal access tokens → Generate new token
  - 权限选择：`repo`

### 步骤 3：部署到 Railway

1. **访问 Railway**
   - 打开：https://railway.app
   - 点击 "Start a New Project"

2. **登录**
   - 选择 "Login with GitHub"
   - 授权 Railway 访问你的 GitHub 账号

3. **部署项目**
   - 点击 "New Project"
   - 选择 "Deploy from GitHub repo"
   - 选择你的 `ai-sales-coach` 仓库
   - Railway 会自动开始部署

4. **等待部署完成**
   - 大约 2-5 分钟
   - 可以在 "Deployments" 标签查看进度

5. **获得 URL**
   - 部署完成后，点击项目
   - 点击 "Settings" → "Domains"
   - 会看到类似：`https://ai-sales-coach-production.up.railway.app`
   - **这就是你的公开 URL！**

### 步骤 4：配置环境变量（可选）

如果需要使用 OpenAI API：

1. 在 Railway 项目页面
2. 点击 "Variables" 标签
3. 点击 "New Variable"
4. 添加：
   - Name: `OPENAI_API_KEY`
   - Value: `你的API密钥`
5. 再添加：
   - Name: `OPENAI_MODEL`
   - Value: `gpt-3.5-turbo`
6. Railway 会自动重新部署

---

## 🎉 完成！

部署完成后：
- ✅ 获得公开 URL（例如：`https://ai-sales-coach-production.up.railway.app`）
- ✅ 朋友可以从任何地方访问
- ✅ 无需你的电脑运行
- ✅ 24/7 在线
- ✅ 自动 HTTPS（安全）

---

## 💰 费用说明

**Railway 免费额度**：
- 每月 **$5 免费额度**
- 对于测试和小规模使用完全够用
- 可以设置使用上限，避免意外费用

**如果担心费用**：
- 在 Railway 设置中启用 "Usage Limits"
- 或使用 Render（完全免费，但会休眠）

---

## 🔄 后续更新代码

代码更新后，只需：
```bash
git add .
git commit -m "更新说明"
git push
```

Railway 会自动检测并重新部署！

---

## ⚠️ 如果遇到问题

1. **Git 推送失败**：
   - 检查 GitHub 用户名是否正确
   - 使用 Personal Access Token 而不是密码

2. **Railway 部署失败**：
   - 检查部署日志
   - 确认 `requirements.txt` 和 `Procfile` 存在

3. **无法访问**：
   - 检查域名是否正确生成
   - 等待几分钟让 DNS 生效

---

## 📞 需要帮助？

如果遇到问题，告诉我具体的错误信息，我会帮你解决！

