# 📤 推送代码到 GitHub - 详细说明

## 方法 1：使用脚本（最简单）

我已经为你创建了一个脚本，只需要运行：

```bash
./push_to_github.sh
```

脚本会：
1. 询问你的 GitHub 用户名
2. 自动连接仓库
3. 自动推送代码

---

## 方法 2：手动执行（如果你知道用户名）

如果你的 GitHub 用户名是 `yourusername`，运行：

```bash
git remote add origin https://github.com/yourusername/ai-sales-coach.git
git branch -M main
git push -u origin main
```

---

## ⚠️ 重要：关于密码

GitHub 现在**不允许使用账号密码**推送，需要使用 **Personal Access Token**：

### 获取 Token 步骤：

1. **访问 GitHub**
   - 打开：https://github.com/settings/tokens
   - 或：GitHub → 右上角头像 → Settings → Developer settings → Personal access tokens → Tokens (classic)

2. **生成新 Token**
   - 点击 "Generate new token" → "Generate new token (classic)"
   - Note（备注）：输入 "Railway Deployment"
   - Expiration（过期时间）：选择 "90 days" 或 "No expiration"
   - Select scopes（权限）：勾选 `repo`（全部仓库权限）
   - 点击 "Generate token"

3. **复制 Token**
   - ⚠️ **重要**：Token 只显示一次，立即复制保存
   - 类似：`ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

4. **使用 Token**
   - 当终端提示输入密码时
   - 用户名：输入你的 GitHub 用户名
   - 密码：**粘贴刚才复制的 Token**（不是你的账号密码）

---

## 🎯 完整流程

1. ✅ 在 GitHub 创建仓库（已完成）
2. 🔄 运行 `./push_to_github.sh` 或手动执行命令
3. 输入 GitHub 用户名
4. 如果提示输入密码，使用 Personal Access Token
5. 等待推送完成

---

## ✅ 验证推送成功

推送成功后，访问你的 GitHub 仓库：
```
https://github.com/你的用户名/ai-sales-coach
```

你应该能看到所有文件都在那里了！

---

## 🆘 如果遇到问题

**问题1：提示 "remote origin already exists"**
```bash
git remote remove origin
git remote add origin https://github.com/你的用户名/ai-sales-coach.git
```

**问题2：提示 "authentication failed"**
- 确认使用 Personal Access Token 而不是密码
- 确认 Token 权限包含 `repo`

**问题3：提示 "repository not found"**
- 检查仓库名是否正确
- 确认仓库是 Public 或你有访问权限

---

## 📝 下一步

推送成功后，就可以到 Railway 部署了！

